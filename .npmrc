auto-install-peers=true
# Hoist @types/* so TypeScript can resolve transitive type dependencies
public-hoist-pattern[]=@types/*

# Auth token for publishing — populated via NPM_TOKEN env var in CI
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
